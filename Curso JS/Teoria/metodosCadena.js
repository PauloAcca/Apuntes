concat() 
// junta dos o mas cadenas y retorna una nueva.
startsWith() 
// si un acadena comienza con los caracteres de otra cadena, devuelve true sino false.
includes() 
// si una cadena puede encontrarse dentro de otra cadena, devuelve true sino false.
indexOf() 
// devuelve el indice del primer caracter de la cadena, si no existe, devuelve -1.
lastIndexOf()
// devuelve el ultimo indice del primer caracter de la cadena sino existe devulve -1.

padStart(cantidad-de-caracteres, "caracteres")
// rellena la cadena con los caracteres elegidos al INICIO hasta llegar a la cantidad seleccionada.
padEnd(cantidad-de-caracteres, "caracteres")
// lo mismo que padStart pero al FINAL.
repeat()
// repite una cadena la cantidad de veces que quieras.

split(con-que-queres-que-lo-divida) 
// divide la cadena como le pidamos.
substring(donde-arranca, donde-termina)
// nos retorna un pedazo de cadena que se combierte en un nuevo string.
toLowerCase()
// convierte a minuscula.
toUpperCase()
// convierte a mayuscula.
toString()
// convierte a string.
trim()
// saca los espacios.
trimStart/End()
// saca los espacios del principio o final
